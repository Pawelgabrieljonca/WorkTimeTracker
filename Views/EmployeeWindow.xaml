<Window x:Class="WorkTimeTracker.Views.EmployeeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Title="Panel Pracownika" Height="400" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <TextBlock FontSize="16" FontWeight="Bold" x:Name="HeaderText">Panel Pracownika</TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 8">
            <Label Width="80">Data:</Label>
            <DatePicker x:Name="EntryDatePicker" SelectedDate="{x:Static sys:DateTime.Today}" />
            <Label Width="80" Margin="12 0 0 0">Godziny:</Label>
            <TextBox x:Name="HoursBox" Width="60" />
            <CheckBox x:Name="IsLeaveCheck" Content="Urlop" Margin="12 0 0 0" />
            <CheckBox x:Name="IsOvertimeCheck" Content="Nadgodziny" Margin="8 0 0 0" />
            <Button Content="Dodaj wpis" Click="AddEntryButton_Click" Margin="12 0 0 0" />
        </StackPanel>

        <GroupBox Header="Moje wpisy" Grid.Row="2" Margin="0 8 0 0">
            <ListBox x:Name="EntriesList" />
        </GroupBox>
    </Grid>
</Window>
